//random number between 19-120 is displayed

var magicNumber = Math.floor(Math.random() * 102) + 19;
var counter = 0;
var wins = 0;
var losses = 0;

//display magic number on the page

$("#magic-number").append(magicNumber);

console.log("Magic number generated by computer = " + magicNumber);


//clicking on a button "crystal" will add a specific amt of points (1-12) to the player's total score
var crystal1 = Math.floor(Math.random() * 12) + 1;
var crystal2 = Math.floor(Math.random() * 12) + 1;
var crystal3 = Math.floor(Math.random() * 12) + 1;
var crystal4 = Math.floor(Math.random() * 12) + 1;

$("#crystal1").attr("magicValue", crystal1);
$("#crystal2").attr("magicValue", crystal2);
$("#crystal3").attr("magicValue", crystal3);
$("#crystal4").attr("magicValue", crystal4);


 function winCheck() {
    if (counter > magicNumber) {
        losses++
        $("#losses").text(losses)
        //update losses and then html
        alert("You unsurprisingly lost.")
        //call reset

    }
    else if (counter == magicNumber) {
        wins++
        $("#wins").text(wins)
        //updates wins and html
        alert("You won!")
        //call reset
    }
 }

 function reset(){ {

    //reset function will eventually go here :)

}}


$("#crystal1").on("click", function() {

    counter += crystal1;
    $("#player-score").text(counter);
    
   winCheck();
   reset();

})
   
    // onClick function where the player's score counter will be updated
    $("#crystal2").on("click", function() {

        counter += crystal2;
        $("#player-score").text(counter);

        winCheck();
        reset();
    
    })
// 
    $("#crystal3").on("click", function() {

    counter += crystal3;
    $("#player-score").text(counter);

    winCheck();
    reset();


    })

$("#crystal4").on("click", function() {

    counter += crystal4;
    $("#player-score").text(counter);

    winCheck();
    reset();


})


//wins & losses
